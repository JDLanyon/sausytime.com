# Cloudflare Pages config for @cloudflare/next-on-pages
# Deployment is handled via GitHub Actions -> Cloudflare Pages

name = "sausytime-com"
compatibility_date = "2025-06-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"
